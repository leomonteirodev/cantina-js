// import Cashier from '../models/Cashier';

class CashierController {
  async index({ res }) {
    return res.json();
  }

  async store(req, res) {
    return res.json();
  }
}

export default new CashierController();
